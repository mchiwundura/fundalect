describe("End of deck utilities", () => {
  it("Sets the performance of the deck to Async storage", () => {});

  it("Sets the Activity to Async storage", () => {});

  it("Appends the streak", () => {});
});
